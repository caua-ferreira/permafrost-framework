# Core
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=12.0.0
zstandard>=0.21.0
duckdb>=0.9.0
typer>=0.9.0
rich>=13.0.0

# Cluster
fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.24.0

# Cloud Storage (opcionais)
# pip install permafrost-framework[s3]     → boto3
# pip install permafrost-framework[gcs]    → google-cloud-storage
# pip install permafrost-framework[azure]  → azure-storage-blob
