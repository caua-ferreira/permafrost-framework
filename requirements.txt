# Core (obrigatório)
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=12.0.0
zstandard>=0.21.0
duckdb>=0.9.0
typer>=0.9.0
rich>=13.0.0

# Cloud Storage (opcionais — instalar conforme necessidade)
# pip install permafrost-framework[s3]
# pip install permafrost-framework[gcs]
# pip install permafrost-framework[azure]
# pip install permafrost-framework[all-cloud]
boto3>=1.26.0                  # AWS S3
google-cloud-storage>=2.10.0  # Google GCS
azure-storage-blob>=12.17.0   # Azure Blob
